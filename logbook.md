# wkhtmltox-rs — logbook

Chronological record of the autonomous build. Newest at bottom. Branch: `codex/macos-arm64-wkhtmltopdf` → `fork`.

## 2026-06-28

- **Decision (engine):** brainstorm → `/devils-advocate` (4 disjoint challengers) → 2 spikes reversed per-platform-WebKit → **single headless Chromium over CDP**. SPIKE 1: Chromium `printToPDF{generateDocumentOutline}` gives correct bookmarks + page-accurate destinations. SPIKE 2: `WKWebView.createPDF` can't paginate → native WebKit dropped. Spec: `docs/superpowers/specs/2026-06-28-no-qt-engine-design.md`.
- **M1 complete** (15 commits `c7e14a0..5248f62`, final-review fixes `bb8feae`): Cargo workspace (core/render-chromium/pdf-sys), `Renderer` trait + `MockRenderer`, Chromium/CDP renderer (gated e2e: paginated PDF + `/Outlines`), QPDF FFI shim, AcroForm spike (top risk retired), fidelity harness vs 0.12.6 oracle. 2 unsafe edges only.
- **Fidelity decision (data-driven):** oracle = gold standard for all tests; no-Qt + opt-in legacy-compat profile. Text+outline already identical; visual SSIM 0.72→tunable; pagination font-metric-bound.
- **Security hook:** `scripts/security-audit.sh` + PostToolUse hook (`feat:`-gated, asyncRewake on HIGH). Caught + fixed **RUSTSEC-2026-0187** (lopdf 0.34 stack overflow → 0.42).
- **Compat-profile v1** (`b168286`): `--compat` UA-reset injection. SSIM 0.717→0.734 (baseline); page-drift unchanged (font-bound). Security audit clean.
- Pushed through `7279083`.
- **M2a (document assembly) started** (BASE `5b6f523`). Execution model: light controller check on low-risk pure-Rust tasks; full reviewer subagent on FFI/integration tasks; whole-milestone review + security audit + oracle validation before each push.
  - Task 1 ✅ `a3ec621` — outline model (probe JS + heading tree), 2/2 tests, clippy clean.
  - Tasks 2–6 ✅ — QPDF merge `b2b7f78`, nested bookmarks `c45bf0f`, page-number footer `bb77a30`, `assemble_pdf` wiring `85ccc02`, oracle validation `575ac5f`.
  - **M2a complete** (`5b6f523..5088a8d`). Opus milestone review: READY-WITH-FIXES, 0 Critical; injection via DOM titles/footer SAFE (qpdf escaping), FFI backstops verified. Fixed Important security finding: temp-dir CWE-377 → `tempfile::TempDir` (randomized, 0700, RAII cleanup) `5088a8d`. Oracle: outline 12/12 titles match, text 0.9974, pages within ±1. Static audit HIGH=0. Per-heading exact page mapping deferred to M2b (the §5.1 crux).
- **M2b complete** (`938b1f4..5adb9e9`). Exact per-heading pages (lopdf parse of engine outline), TOC + fixed-point page loop, variable headers/footers (cells + tokens), cover page, clickable `/Link` synthesis, `Source::Html` on real Chrome. Opus milestone review: READY-WITH-FIXES, 0 Critical/Important; security pass found NO exploitable issues (TOC HTML-escaped, FFI backstops, temp file O_EXCL/0600, bounds checks). Hardened: lopdf cycle/depth guards. Oracle full-doc: outline 13/13 titles match, TOC present, pages ±2 (text_sim low = TOC/footer formatting artifact, body matches). Static audit HIGH=0. Deferred → post-v1: custom-XSLT TOC, HTML headers, TOC-link wiring, link-rect coordinate accuracy.
- **M3 complete** (`992d3b0..095f961`). Settings registry (~25 impl names + 30 accept-warn), vendored `pdf.h`/`image.h` byte-identical + header-diff test, `wkhtmltox-capi` cdylib = `libwkhtmltox` with all 27 `wkhtmltopdf_*` exports (catch_unwind on every export, per-converter `str_cache` for borrowed `const char*`, ownership-transfer of settings to match upstream). C consumer test PASSES (links libwkhtmltox, %PDF 14KB, finished ok, string-lifetime valid, `leaks`=0). Opus review: READY-WITH-FIXES, 0 Critical, 0 security findings; fixed ownership leak + http_error consistency + safety doc. KNOWN: back-to-back convert in one process can hit a Chrome WebSocket reset on rapid re-spawn → renderer-robustness item for M4 (single conversions fine).
- **M3b complete** (`42b1158..b101c60`). `wkhtmltox-cli` (page-object grammar parser, 54 flags, flag→registry mapping, help/version modes) + `wkhtmltopdf` executable (convert via Chromium, stdin/stdout, help/version). Opus review: READY-WITH-FIXES, 0 Critical, no security blockers (no shell/injection, temp files O_EXCL, no panic-on-bad-input). Fixed: bad-flag-value + missing-input now exit nonzero (match oracle), stdout flush, percent-encode file://. Oracle CLI compat: outline 13/13 titles, exit codes match (success/bad-flag/missing-input). **C ABI + CLI both functional.** Deferred → post-v1: --quiet, full flag parity, manpage exactness.
- **M4 complete** (`237f351..7b3de90`). ResourcePolicy (file://+SSRF gating, redirect re-validation, 101 tests); CDP networking (cookies/headers/auth/cert/proxy); enforcement via CDP Fetch interception + `--safe`; rapid-respawn fixed. **Opus security review found 2 Critical fail-open/bypass + 3 Important — ALL FIXED & verified:** whole-load Fetch enforcement (post-load JS SSRF now blocked — gated test `post_load_ssrf_blocked_whole_load_fetch` ✓), trust-top-level/gate-subresources (so --safe+TOC/cover work), userinfo strip, alt-IP (decimal/hex/octal) + IPv4-mapped IPv6 ranges, per-origin auth headers (no cross-origin leak). 5/5 gated tests pass; --safe VERIFIED fail-closed. Follow-up: re-audit call_pumping in M6. The security review + fix loop did its job — caught real CVE-class bypasses before merge.
- **M6 complete** (`c59ceea..9b9088b`). Compat hardening, executed via SDD (5 tasks, fresh implementer + reviewer subagent each, milestone gate). **T1** oracle harness gained ordered outline-tree similarity (`outline_tree_ratio`, difflib over `(level,title)` seqs) + body-only text metric (`extract_body_text`, clips header/footer margin bands + leading TOC) + `--gate` threshold check (`thresholds.json`); review fixed a real CLI-typo-swallowing regression (`parse_known_args`→strict `parse_args()`) + wired body-text into the `--m2b` path. **T2** `wkhtmltoimage` exit-code parity (missing local input → nonzero; guard pre-existed from M5, pinned by a stderr-asserting integration test). **T3** image device-metrics forwarding — `--zoom`→`deviceScaleFactor`, `screenWidth/Height/smartWidth`→`Emulation.setDeviceMetricsOverride`, `--no-images`→Fetch `Image`-block, JPEG alpha-over-white composite; gated e2e on real Chrome confirmed width=800. **T4** forms→AcroForm end-to-end (text inputs): `forms.rs` probe+parse, pdf-sys `add_text_fields` TempDir wrapper over the path-based shim, assembly wiring reusing the byte-identical link px→pt transform, `--enable-forms`; gated e2e: email+note→2 AcroForm fields on real Chrome. **T5** realistic self-contained legacy corpus (invoice/report/article) + recorded results (mean_ssim 0.5477, text 0.96–1.00 — divergence is cross-engine font rendering + pagination drift). **Opus milestone review: READY, 0 Critical/Important; the mandated `call_pumping` re-audit PASSED** — the new image-block is monotonic (fires after C1b, before policy, only `failRequest`; symmetric across both `requestPaused` handlers → can only add blocks, never convert a policy Block into Allow), the smart_width `Runtime.evaluate` round-trips keep the Fetch pump live and are `CALL_TIMEOUT`-bounded, the form probe runs with Fetch already disabled (no network); M4 redirect re-validation / per-origin headers / userinfo strip / Document-allow all byte-for-byte intact. Static audit HIGH=0 (4 MED are known generic-surface heuristics; nothing M6-specific flagged). Gate fix wave (`9b9088b`): raw FFI symbol made private (acroform spike test switched to the safe wrapper), test-only CWE-377 → `NamedTempFile`, degenerate-rect test coverage, M6 test clippy lints, thresholds corpus-note. Deferred → post-v1: `--no-images` for the **PDF** path (M6 wired images only).
- **M5 complete** (`2e9e0bc..3699508`). HTML→image: `ChromiumRenderer::snapshot` (CDP `Page.captureScreenshot`, full-page or `--crop-*`) routed through the M4 whole-load Fetch/policy pump; core `image::produce` (decode→crop→resize→alpha-flatten→encode via the `image` crate, `forbid(unsafe)`); `ImageGlobalSettings` + `registry::set_image_global`; the `wkhtmltoimage_*` C ABI (18 exports, same catch_unwind + per-converter str_cache + ownership-transfer pattern as the PDF ABI; `image.h` vendored); the `wkhtmltoimage` CLI. Gated: image C consumer PNG, leaks=0; `snapshot_under_safe_policy_blocks_private_ip_subresource` ✓ (proves `--safe` covers image render, capture still succeeds). **Opus milestone review: READY-WITH-FIXES, 0 Critical, 0 new security findings** — snapshot policy enforcement correct (open→wait_ready→snapshot all via `call_pumping`, Fetch disabled after capture like `print_pdf`), FFI parity with the PDF side confirmed, `produce` panic-safe on hostile bytes (`load_from_memory` errors propagated, `crop_imm` clamps). **Fixed:** double-crop (CDP `clip` already cropped, then `produce` re-cropped → both consumers now null `crop` before `produce`); `file://` percent-encoding in the C ABI (parity with CLI); one M5 clippy hit. 14/14 core-image + 34/34 CLI tests pass; clippy clean in touched files. Oracle (`--image`): both binaries emit valid PNGs; SSIM/text low where the macOS-arm64 oracle renders pages black (oracle bug, not ours); width 756 vs 800 (smart-width). Static audit HIGH=0. **Deferred → M6** (stored-not-forwarded image settings): `--zoom` (no-op unless crop set), `--no-images`/`web.loadImages` (accepted-ignored), `screen_height`/`smart_width`, JPEG alpha-over-white compositing.
